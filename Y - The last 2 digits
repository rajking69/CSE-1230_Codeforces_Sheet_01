#include<stdio.h>
int main()
{
    long long int a,b,c,d,z,result;
    scanf("%lld %lld %lld %lld",&a,&b,&c,&d);
    z=(a%100)*(b%100)*(c%100)*(d%100);
    result = z%100;
    if(result>=10 && result<=99)
        printf("%lld\n",result);
    else if(result>=0 && result<=9)
        printf("0%lld\n",result);
    return 0;
}
